/*
 * Kadastraal onroerende zaken
 * D.m.v. deze toepassing worden meerdere, korte bevragingen op de Basis Registratie Kadaster beschikbaar gesteld. Deze toepassing betreft het verstrekken van Kadastrale Onroerende Zaak informatie.
 *
 * OpenAPI spec version: 1.0.0
 * 
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 */

package io.swagger.client;

import com.google.gson.Gson;
import com.google.gson.GsonBuilder;
import com.google.gson.JsonElement;
import com.google.gson.JsonObject;
import io.gsonfire.GsonFireBuilder;
import io.gsonfire.PostProcessor;
import io.gsonfire.TypeSelector;
import com.google.gson.JsonParseException;
import com.google.gson.TypeAdapter;
import com.google.gson.internal.bind.util.ISO8601Utils;
import com.google.gson.stream.JsonReader;
import com.google.gson.stream.JsonWriter;
import org.threeten.bp.LocalDate;
import org.threeten.bp.OffsetDateTime;
import org.threeten.bp.format.DateTimeFormatter;

import io.swagger.client.model.*;

import java.io.IOException;
import java.io.StringReader;
import java.lang.reflect.Type;
import java.text.DateFormat;
import java.text.ParseException;
import java.text.ParsePosition;
import java.util.Date;
import java.util.Map;
import java.util.HashMap;

public class JSON {
    private Gson gson;
    private boolean isLenientOnJson = false;
    private DateTypeAdapter dateTypeAdapter = new DateTypeAdapter();
    private SqlDateTypeAdapter sqlDateTypeAdapter = new SqlDateTypeAdapter();
    private OffsetDateTimeTypeAdapter offsetDateTimeTypeAdapter = new OffsetDateTimeTypeAdapter();
    private LocalDateTypeAdapter localDateTypeAdapter = new LocalDateTypeAdapter();

    public static GsonBuilder createGson() {
        GsonFireBuilder fireBuilder = new GsonFireBuilder()
          .registerTypeSelector(Foutbericht.class, new TypeSelector<Foutbericht>() {
            @Override
            public Class<? extends Foutbericht> getClassForElement(JsonElement readElement) {
                Map<String, Class<? extends Foutbericht>> classByDiscriminatorValue = new HashMap<>();
                    classByDiscriminatorValue.put("BadRequestFoutbericht".toUpperCase(), BadRequestFoutbericht.class);
                    classByDiscriminatorValue.put("Foutbericht".toUpperCase(), Foutbericht.class);
                return getClassByDiscriminator(
                            classByDiscriminatorValue,
                            getDiscriminatorValue(readElement, ""));
            }
          })
          .registerPostProcessor(Foutbericht.class, new PostProcessor<Foutbericht>() {
              @Override
              public void postDeserialize(Foutbericht result, JsonElement src, Gson gson) {

              }

              @Override
              public void postSerialize(JsonElement result, Foutbericht src, Gson gson) {
                  Map<Class<? extends Foutbericht>, String> discriminatorValueByClass = new HashMap<>();
                      discriminatorValueByClass.put(BadRequestFoutbericht.class, "BadRequestFoutbericht");
                      discriminatorValueByClass.put(Foutbericht.class, "Foutbericht");
                  if(result instanceof JsonObject)
                  {
                      if(!((JsonObject) result).has(""))
                      {
                          ((JsonObject) result).addProperty("", discriminatorValueByClass.get(src.getClass()));
                      }
                  }
              }
          })
          .registerTypeSelector(KadasterPersoon.class, new TypeSelector<KadasterPersoon>() {
            @Override
            public Class<? extends KadasterPersoon> getClassForElement(JsonElement readElement) {
                Map<String, Class<? extends KadasterPersoon>> classByDiscriminatorValue = new HashMap<>();
                    classByDiscriminatorValue.put("KadasterNatuurlijkPersoon".toUpperCase(), KadasterNatuurlijkPersoon.class);
                    classByDiscriminatorValue.put("KadasterNietNatuurlijkPersoon".toUpperCase(), KadasterNietNatuurlijkPersoon.class);
                    classByDiscriminatorValue.put("KadasterPersoon".toUpperCase(), KadasterPersoon.class);
                return getClassByDiscriminator(
                            classByDiscriminatorValue,
                            getDiscriminatorValue(readElement, ""));
            }
          })
          .registerPostProcessor(KadasterPersoon.class, new PostProcessor<KadasterPersoon>() {
              @Override
              public void postDeserialize(KadasterPersoon result, JsonElement src, Gson gson) {

              }

              @Override
              public void postSerialize(JsonElement result, KadasterPersoon src, Gson gson) {
                  Map<Class<? extends KadasterPersoon>, String> discriminatorValueByClass = new HashMap<>();
                      discriminatorValueByClass.put(KadasterNatuurlijkPersoon.class, "KadasterNatuurlijkPersoon");
                      discriminatorValueByClass.put(KadasterNietNatuurlijkPersoon.class, "KadasterNietNatuurlijkPersoon");
                      discriminatorValueByClass.put(KadasterPersoon.class, "KadasterPersoon");
                  if(result instanceof JsonObject)
                  {
                      if(!((JsonObject) result).has(""))
                      {
                          ((JsonObject) result).addProperty("", discriminatorValueByClass.get(src.getClass()));
                      }
                  }
              }
          })
          .registerTypeSelector(KadasterNatuurlijkPersoon.class, new TypeSelector<KadasterNatuurlijkPersoon>() {
            @Override
            public Class<? extends KadasterNatuurlijkPersoon> getClassForElement(JsonElement readElement) {
                Map<String, Class<? extends KadasterNatuurlijkPersoon>> classByDiscriminatorValue = new HashMap<>();
                    classByDiscriminatorValue.put("KadasterNatuurlijkPersoonHal".toUpperCase(), KadasterNatuurlijkPersoonHal.class);
                    classByDiscriminatorValue.put("KadasterNatuurlijkPersoon".toUpperCase(), KadasterNatuurlijkPersoon.class);
                return getClassByDiscriminator(
                            classByDiscriminatorValue,
                            getDiscriminatorValue(readElement, ""));
            }
          })
          .registerPostProcessor(KadasterNatuurlijkPersoon.class, new PostProcessor<KadasterNatuurlijkPersoon>() {
              @Override
              public void postDeserialize(KadasterNatuurlijkPersoon result, JsonElement src, Gson gson) {

              }

              @Override
              public void postSerialize(JsonElement result, KadasterNatuurlijkPersoon src, Gson gson) {
                  Map<Class<? extends KadasterNatuurlijkPersoon>, String> discriminatorValueByClass = new HashMap<>();
                      discriminatorValueByClass.put(KadasterNatuurlijkPersoonHal.class, "KadasterNatuurlijkPersoonHal");
                      discriminatorValueByClass.put(KadasterNatuurlijkPersoon.class, "KadasterNatuurlijkPersoon");
                  if(result instanceof JsonObject)
                  {
                      if(!((JsonObject) result).has(""))
                      {
                          ((JsonObject) result).addProperty("", discriminatorValueByClass.get(src.getClass()));
                      }
                  }
              }
          })
          .registerTypeSelector(KadasterNietNatuurlijkPersoon.class, new TypeSelector<KadasterNietNatuurlijkPersoon>() {
            @Override
            public Class<? extends KadasterNietNatuurlijkPersoon> getClassForElement(JsonElement readElement) {
                Map<String, Class<? extends KadasterNietNatuurlijkPersoon>> classByDiscriminatorValue = new HashMap<>();
                    classByDiscriminatorValue.put("KadasterNietNatuurlijkPersoonHal".toUpperCase(), KadasterNietNatuurlijkPersoonHal.class);
                    classByDiscriminatorValue.put("KadasterNietNatuurlijkPersoon".toUpperCase(), KadasterNietNatuurlijkPersoon.class);
                return getClassByDiscriminator(
                            classByDiscriminatorValue,
                            getDiscriminatorValue(readElement, ""));
            }
          })
          .registerPostProcessor(KadasterNietNatuurlijkPersoon.class, new PostProcessor<KadasterNietNatuurlijkPersoon>() {
              @Override
              public void postDeserialize(KadasterNietNatuurlijkPersoon result, JsonElement src, Gson gson) {

              }

              @Override
              public void postSerialize(JsonElement result, KadasterNietNatuurlijkPersoon src, Gson gson) {
                  Map<Class<? extends KadasterNietNatuurlijkPersoon>, String> discriminatorValueByClass = new HashMap<>();
                      discriminatorValueByClass.put(KadasterNietNatuurlijkPersoonHal.class, "KadasterNietNatuurlijkPersoonHal");
                      discriminatorValueByClass.put(KadasterNietNatuurlijkPersoon.class, "KadasterNietNatuurlijkPersoon");
                  if(result instanceof JsonObject)
                  {
                      if(!((JsonObject) result).has(""))
                      {
                          ((JsonObject) result).addProperty("", discriminatorValueByClass.get(src.getClass()));
                      }
                  }
              }
          })
          .registerTypeSelector(PersoonBasis.class, new TypeSelector<PersoonBasis>() {
            @Override
            public Class<? extends PersoonBasis> getClassForElement(JsonElement readElement) {
                Map<String, Class<? extends PersoonBasis>> classByDiscriminatorValue = new HashMap<>();
                    classByDiscriminatorValue.put("KadasterPersoon".toUpperCase(), KadasterPersoon.class);
                    classByDiscriminatorValue.put("NatuurlijkPersoonBeperkt".toUpperCase(), NatuurlijkPersoonBeperkt.class);
                    classByDiscriminatorValue.put("NietNatuurlijkPersoonBeperkt".toUpperCase(), NietNatuurlijkPersoonBeperkt.class);
                    classByDiscriminatorValue.put("PersoonBeperkt".toUpperCase(), PersoonBeperkt.class);
                    classByDiscriminatorValue.put("PersoonBasis".toUpperCase(), PersoonBasis.class);
                return getClassByDiscriminator(
                            classByDiscriminatorValue,
                            getDiscriminatorValue(readElement, ""));
            }
          })
          .registerPostProcessor(PersoonBasis.class, new PostProcessor<PersoonBasis>() {
              @Override
              public void postDeserialize(PersoonBasis result, JsonElement src, Gson gson) {

              }

              @Override
              public void postSerialize(JsonElement result, PersoonBasis src, Gson gson) {
                  Map<Class<? extends PersoonBasis>, String> discriminatorValueByClass = new HashMap<>();
                      discriminatorValueByClass.put(KadasterPersoon.class, "KadasterPersoon");
                      discriminatorValueByClass.put(NatuurlijkPersoonBeperkt.class, "NatuurlijkPersoonBeperkt");
                      discriminatorValueByClass.put(NietNatuurlijkPersoonBeperkt.class, "NietNatuurlijkPersoonBeperkt");
                      discriminatorValueByClass.put(PersoonBeperkt.class, "PersoonBeperkt");
                      discriminatorValueByClass.put(PersoonBasis.class, "PersoonBasis");
                  if(result instanceof JsonObject)
                  {
                      if(!((JsonObject) result).has(""))
                      {
                          ((JsonObject) result).addProperty("", discriminatorValueByClass.get(src.getClass()));
                      }
                  }
              }
          })
          .registerTypeSelector(KadastraalOnroerendeZaak.class, new TypeSelector<KadastraalOnroerendeZaak>() {
            @Override
            public Class<? extends KadastraalOnroerendeZaak> getClassForElement(JsonElement readElement) {
                Map<String, Class<? extends KadastraalOnroerendeZaak>> classByDiscriminatorValue = new HashMap<>();
                    classByDiscriminatorValue.put("KadastraalOnroerendeZaakHal".toUpperCase(), KadastraalOnroerendeZaakHal.class);
                    classByDiscriminatorValue.put("KadastraalOnroerendeZaak".toUpperCase(), KadastraalOnroerendeZaak.class);
                return getClassByDiscriminator(
                            classByDiscriminatorValue,
                            getDiscriminatorValue(readElement, ""));
            }
          })
          .registerPostProcessor(KadastraalOnroerendeZaak.class, new PostProcessor<KadastraalOnroerendeZaak>() {
              @Override
              public void postDeserialize(KadastraalOnroerendeZaak result, JsonElement src, Gson gson) {

              }

              @Override
              public void postSerialize(JsonElement result, KadastraalOnroerendeZaak src, Gson gson) {
                  Map<Class<? extends KadastraalOnroerendeZaak>, String> discriminatorValueByClass = new HashMap<>();
                      discriminatorValueByClass.put(KadastraalOnroerendeZaakHal.class, "KadastraalOnroerendeZaakHal");
                      discriminatorValueByClass.put(KadastraalOnroerendeZaak.class, "KadastraalOnroerendeZaak");
                  if(result instanceof JsonObject)
                  {
                      if(!((JsonObject) result).has(""))
                      {
                          ((JsonObject) result).addProperty("", discriminatorValueByClass.get(src.getClass()));
                      }
                  }
              }
          })
          .registerTypeSelector(Adres.class, new TypeSelector<Adres>() {
            @Override
            public Class<? extends Adres> getClassForElement(JsonElement readElement) {
                Map<String, Class<? extends Adres>> classByDiscriminatorValue = new HashMap<>();
                    classByDiscriminatorValue.put("Postadres".toUpperCase(), Postadres.class);
                    classByDiscriminatorValue.put("Adres".toUpperCase(), Adres.class);
                return getClassByDiscriminator(
                            classByDiscriminatorValue,
                            getDiscriminatorValue(readElement, ""));
            }
          })
          .registerPostProcessor(Adres.class, new PostProcessor<Adres>() {
              @Override
              public void postDeserialize(Adres result, JsonElement src, Gson gson) {

              }

              @Override
              public void postSerialize(JsonElement result, Adres src, Gson gson) {
                  Map<Class<? extends Adres>, String> discriminatorValueByClass = new HashMap<>();
                      discriminatorValueByClass.put(Postadres.class, "Postadres");
                      discriminatorValueByClass.put(Adres.class, "Adres");
                  if(result instanceof JsonObject)
                  {
                      if(!((JsonObject) result).has(""))
                      {
                          ((JsonObject) result).addProperty("", discriminatorValueByClass.get(src.getClass()));
                      }
                  }
              }
          })
          .registerTypeSelector(ZakelijkGerechtigde.class, new TypeSelector<ZakelijkGerechtigde>() {
            @Override
            public Class<? extends ZakelijkGerechtigde> getClassForElement(JsonElement readElement) {
                Map<String, Class<? extends ZakelijkGerechtigde>> classByDiscriminatorValue = new HashMap<>();
                    classByDiscriminatorValue.put("ZakelijkGerechtigdeHal".toUpperCase(), ZakelijkGerechtigdeHal.class);
                    classByDiscriminatorValue.put("ZakelijkGerechtigde".toUpperCase(), ZakelijkGerechtigde.class);
                return getClassByDiscriminator(
                            classByDiscriminatorValue,
                            getDiscriminatorValue(readElement, ""));
            }
          })
          .registerPostProcessor(ZakelijkGerechtigde.class, new PostProcessor<ZakelijkGerechtigde>() {
              @Override
              public void postDeserialize(ZakelijkGerechtigde result, JsonElement src, Gson gson) {

              }

              @Override
              public void postSerialize(JsonElement result, ZakelijkGerechtigde src, Gson gson) {
                  Map<Class<? extends ZakelijkGerechtigde>, String> discriminatorValueByClass = new HashMap<>();
                      discriminatorValueByClass.put(ZakelijkGerechtigdeHal.class, "ZakelijkGerechtigdeHal");
                      discriminatorValueByClass.put(ZakelijkGerechtigde.class, "ZakelijkGerechtigde");
                  if(result instanceof JsonObject)
                  {
                      if(!((JsonObject) result).has(""))
                      {
                          ((JsonObject) result).addProperty("", discriminatorValueByClass.get(src.getClass()));
                      }
                  }
              }
          })
        ;
        return fireBuilder.createGsonBuilder();
    }

    private static String getDiscriminatorValue(JsonElement readElement, String discriminatorField) {
        JsonElement element = readElement.getAsJsonObject().get(discriminatorField);
        if(null == element) {
            throw new IllegalArgumentException("missing discriminator field: <" + discriminatorField + ">");
        }
        return element.getAsString();
    }

    private static <T> Class<? extends T> getClassByDiscriminator(Map<String, Class<? extends T>> classByDiscriminatorValue, String discriminatorValue) {
        Class<? extends T> clazz = classByDiscriminatorValue.get(discriminatorValue.toUpperCase());
        if(null == clazz) {
            throw new IllegalArgumentException("cannot determine model class of name: <" + discriminatorValue + ">");
        }
        return clazz;
    }

    public JSON() {
        gson = createGson()
            .registerTypeAdapter(Date.class, dateTypeAdapter)
            .registerTypeAdapter(java.sql.Date.class, sqlDateTypeAdapter)
            .registerTypeAdapter(OffsetDateTime.class, offsetDateTimeTypeAdapter)
            .registerTypeAdapter(LocalDate.class, localDateTypeAdapter)
            .create();
    }

    /**
     * Get Gson.
     *
     * @return Gson
     */
    public Gson getGson() {
        return gson;
    }

    /**
     * Set Gson.
     *
     * @param gson Gson
     * @return JSON
     */
    public JSON setGson(Gson gson) {
        this.gson = gson;
        return this;
    }

    public JSON setLenientOnJson(boolean lenientOnJson) {
        isLenientOnJson = lenientOnJson;
        return this;
    }

    /**
     * Serialize the given Java object into JSON string.
     *
     * @param obj Object
     * @return String representation of the JSON
     */
    public String serialize(Object obj) {
        return gson.toJson(obj);
    }

    /**
     * Deserialize the given JSON string to Java object.
     *
     * @param <T>        Type
     * @param body       The JSON string
     * @param returnType The type to deserialize into
     * @return The deserialized Java object
     */
    @SuppressWarnings("unchecked")
    public <T> T deserialize(String body, Type returnType) {
        try {
            if (isLenientOnJson) {
                JsonReader jsonReader = new JsonReader(new StringReader(body));
                // see https://google-gson.googlecode.com/svn/trunk/gson/docs/javadocs/com/google/gson/stream/JsonReader.html#setLenient(boolean)
                jsonReader.setLenient(true);
                return gson.fromJson(jsonReader, returnType);
            } else {
                return gson.fromJson(body, returnType);
            }
        } catch (JsonParseException e) {
            // Fallback processing when failed to parse JSON form response body:
            // return the response body string directly for the String return type;
            if (returnType.equals(String.class))
                return (T) body;
            else throw (e);
        }
    }

    /**
     * Gson TypeAdapter for JSR310 OffsetDateTime type
     */
    public static class OffsetDateTimeTypeAdapter extends TypeAdapter<OffsetDateTime> {

        private DateTimeFormatter formatter;

        public OffsetDateTimeTypeAdapter() {
            this(DateTimeFormatter.ISO_OFFSET_DATE_TIME);
        }

        public OffsetDateTimeTypeAdapter(DateTimeFormatter formatter) {
            this.formatter = formatter;
        }

        public void setFormat(DateTimeFormatter dateFormat) {
            this.formatter = dateFormat;
        }

        @Override
        public void write(JsonWriter out, OffsetDateTime date) throws IOException {
            if (date == null) {
                out.nullValue();
            } else {
                out.value(formatter.format(date));
            }
        }

        @Override
        public OffsetDateTime read(JsonReader in) throws IOException {
            switch (in.peek()) {
                case NULL:
                    in.nextNull();
                    return null;
                default:
                    String date = in.nextString();
                    if (date.endsWith("+0000")) {
                        date = date.substring(0, date.length()-5) + "Z";
                    }
                    return OffsetDateTime.parse(date, formatter);
            }
        }
    }

    /**
     * Gson TypeAdapter for JSR310 LocalDate type
     */
    public class LocalDateTypeAdapter extends TypeAdapter<LocalDate> {

        private DateTimeFormatter formatter;

        public LocalDateTypeAdapter() {
            this(DateTimeFormatter.ISO_LOCAL_DATE);
        }

        public LocalDateTypeAdapter(DateTimeFormatter formatter) {
            this.formatter = formatter;
        }

        public void setFormat(DateTimeFormatter dateFormat) {
            this.formatter = dateFormat;
        }

        @Override
        public void write(JsonWriter out, LocalDate date) throws IOException {
            if (date == null) {
                out.nullValue();
            } else {
                out.value(formatter.format(date));
            }
        }

        @Override
        public LocalDate read(JsonReader in) throws IOException {
            switch (in.peek()) {
                case NULL:
                    in.nextNull();
                    return null;
                default:
                    String date = in.nextString();
                    return LocalDate.parse(date, formatter);
            }
        }
    }

    public JSON setOffsetDateTimeFormat(DateTimeFormatter dateFormat) {
        offsetDateTimeTypeAdapter.setFormat(dateFormat);
        return this;
    }

    public JSON setLocalDateFormat(DateTimeFormatter dateFormat) {
        localDateTypeAdapter.setFormat(dateFormat);
        return this;
    }

    /**
     * Gson TypeAdapter for java.sql.Date type
     * If the dateFormat is null, a simple "yyyy-MM-dd" format will be used
     * (more efficient than SimpleDateFormat).
     */
    public static class SqlDateTypeAdapter extends TypeAdapter<java.sql.Date> {

        private DateFormat dateFormat;

        public SqlDateTypeAdapter() {
        }

        public SqlDateTypeAdapter(DateFormat dateFormat) {
            this.dateFormat = dateFormat;
        }

        public void setFormat(DateFormat dateFormat) {
            this.dateFormat = dateFormat;
        }

        @Override
        public void write(JsonWriter out, java.sql.Date date) throws IOException {
            if (date == null) {
                out.nullValue();
            } else {
                String value;
                if (dateFormat != null) {
                    value = dateFormat.format(date);
                } else {
                    value = date.toString();
                }
                out.value(value);
            }
        }

        @Override
        public java.sql.Date read(JsonReader in) throws IOException {
            switch (in.peek()) {
                case NULL:
                    in.nextNull();
                    return null;
                default:
                    String date = in.nextString();
                    try {
                        if (dateFormat != null) {
                            return new java.sql.Date(dateFormat.parse(date).getTime());
                        }
                        return new java.sql.Date(ISO8601Utils.parse(date, new ParsePosition(0)).getTime());
                    } catch (ParseException e) {
                        throw new JsonParseException(e);
                    }
            }
        }
    }

    /**
     * Gson TypeAdapter for java.util.Date type
     * If the dateFormat is null, ISO8601Utils will be used.
     */
    public static class DateTypeAdapter extends TypeAdapter<Date> {

        private DateFormat dateFormat;

        public DateTypeAdapter() {
        }

        public DateTypeAdapter(DateFormat dateFormat) {
            this.dateFormat = dateFormat;
        }

        public void setFormat(DateFormat dateFormat) {
            this.dateFormat = dateFormat;
        }

        @Override
        public void write(JsonWriter out, Date date) throws IOException {
            if (date == null) {
                out.nullValue();
            } else {
                String value;
                if (dateFormat != null) {
                    value = dateFormat.format(date);
                } else {
                    value = ISO8601Utils.format(date, true);
                }
                out.value(value);
            }
        }

        @Override
        public Date read(JsonReader in) throws IOException {
            try {
                switch (in.peek()) {
                    case NULL:
                        in.nextNull();
                        return null;
                    default:
                        String date = in.nextString();
                        try {
                            if (dateFormat != null) {
                                return dateFormat.parse(date);
                            }
                            return ISO8601Utils.parse(date, new ParsePosition(0));
                        } catch (ParseException e) {
                            throw new JsonParseException(e);
                        }
                }
            } catch (IllegalArgumentException e) {
                throw new JsonParseException(e);
            }
        }
    }

    public JSON setDateFormat(DateFormat dateFormat) {
        dateTypeAdapter.setFormat(dateFormat);
        return this;
    }

    public JSON setSqlDateFormat(DateFormat dateFormat) {
        sqlDateTypeAdapter.setFormat(dateFormat);
        return this;
    }

}
